<div class="container">

<h1>会員編集画面</h1>

<%= form_for(@customer, url: admins_customers_path) do |f| %>

	<div class="field row">
	  <h4>メールアドレス</h4>
	  <%= f.text_field :email %>
	</div>

  <div class="actions row">
	<%= f.submit '変更を保存する' %>
  </div>

<% end %>

</div>